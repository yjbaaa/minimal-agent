# Role & Goal
你是一位资深 AI 架构师与 Python 核心工程师。你需要帮我从零（Zero-Dependency Runtime）实现一个最小可用（MVP）的 AI Agent 框架与完整项目。
严禁使用任何现有的 Agent 编排框架（如 LangChain, LangGraph, LlamaIndex, OpenHands, AutoGen 等），核心 Agent Runtime、ReAct 循环、状态与 Session 管理必须手写原生代码。仅允许使用官方 LLM Client SDK（如 `openai` / `anthropic`）和基础辅助库（如 `pydantic`）。

---

## 1. 核心架构与功能需求

### (1) Agent Loop (ReAct 运行时)
- **单轮调用时序**：
  1. 接收用户输入，拼装上下文（System Prompt + History + Tools Schema）。
  2. 调用真实 LLM API（基于标准 Tool Call / Function Call 或 JSON ReAct 协议）。
  3. 解析输出：提取思考过程（Thought/Reasoning）、工具调用（Tool Calls）或最终回复（Final Answer）。
  4. 若判定需调用工具：执行工具分发，记录调用 Trace 与执行结果，将结果作为 Tool Message/Observation 追加进 Context。
  5. 循环判断：根据工具返回继续调用下一步，或在拿到充分信息后生成最终答案返回给用户。
  6. 设定最大单次迭代步数（如 `max_steps=5`），防止死循环。

### (2) 工具注册机制 (Tool Registry)
- 实现装饰器或注册类（如 `@tool`），每个工具必须包含：`name`, `description`, `parameters (JSON Schema)`，供 LLM 自动理解。
- 至少实现 3 个内置工具：
  1. `calculator(expression: str)`：安全的数学计算解析（如基于 `numexpr` 或 Python `ast`）。
  2. `search(query: str)`：可 Mock 典型知识库或返回模拟网页检索结果。
  3. `todo_manager(action: str, content: str = "")` 或 `weather(city: str)`：自选可读写的实用工具。
- 完善的异常捕获：工具执行报错时需将标准 Error Message 返回给 LLM，允许 Agent 自我修正，而不是直接 Crash 进程。
- 完整的 Trace 日志：格式化输出每次循环的 `Step`、`Thought`、`Tool Call (args)`、`Execution Time` 和 `Tool Output`。

### (3) Session 与 Context 状态管理
- **Session 隔离**：设计 `SessionManager`，支持通过 `session_id` 区分独立会话（例如用户窗口 1“查天气记待办”与窗口 2“写周报”完全隔离互不串话）。
- **Context 上下文结构**：
  - 维护标准消息流：`SystemMessage`, `UserMessage`, `AssistantMessage`, `ToolMessage`。
  - 支持多轮上下文记忆与追问（包括纯对话追问、带工具调用的多轮追问）。
- **基础 Context 压缩机制**：
  - 支持 `max_history_turns` 限制。
  - 当历史消息达到上限或 token 预警阈值时，实现基础截断/滚动窗口（Sliding Window）或保留头部 System Prompt 与尾部最近 N 轮对话的基础压缩策略。

---

## 2. 工程化与交付产物要求

### (1) 测试用例构建 (`pytest`)
编写端到端的自动化测试：
1. **基础工具测试**：测试 3 个工具的输入输出与异常边界。
2. **多轮对话与追问测试**：纯文字追问是否保留上下文；追问是否能正确串联工具结果。
3. **Session 隔离测试**：模拟窗口 A 与窗口 B 并行对话，验证内存与待办/状态不发生交叉污染。
4. **循环上限与容错测试**：模拟死循环工具或错误工具输入，验证 Agent 优雅退出。

### (2) 文档交付 (`README.md`)
必须生成高质量的 README，包含：
1. **系统架构设计图**（ASCII/Mermaid 形式展示 Agent Loop、Memory 与 Session 流程）。
2. **快速启动指南**（依赖安装、真实 LLM API KEY 配置与运行示例）。
3. **Memory 架构详解**：明确说明 Context 中放置了哪些信息、召回时机、截断压缩机制的放置策略。

---

## 3. 执行要求与代码规范
1. 请先提供清晰的目录结构设计（如 `agent/`, `tools/`, `session/`, `tests/`, `main.py`）。
2. 代码采用现代 Python 3.10+ 风格，包含完整的 Type Hints、Pydantic 校验与详细注释。
3. 请以端到端可运行为目标，逐步生成完整代码，不要省略关键逻辑代码或留空 TODO。